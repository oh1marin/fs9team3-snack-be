# EC2 배포용 env 템플릿 (git pull 시 이 파일은 가져와짐)
# 사용법: EC2에서 cp env.deploy.example .env 후 비밀값만 채우기
# 또는 로컬에서 scp .env ec2-user@서버IP:~/fs9team3-snack-be/.env

PORT=3001
NODE_ENV=production

DATABASE_URL=postgresql://USER:PASSWORD@RDS엔드포인트:5432/DB이름?schema=public

JWT_SECRET=강력한-액세스-시크릿-문자열
JWT_REFRESH_SECRET=강력한-리프레시-시크릿-문자열

CORS_ORIGIN=http://localhost:3000,http://localhost:4000,https://fs9team3-snack-fe.vercel.app,https://marin-snack.store,https://www.marin-snack.store

AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_PUBLIC_BUCKET_NAME=
AWS_PRIVATE_BUCKET_NAME=
